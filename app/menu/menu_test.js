'use strict';

describe('shine.menu module', function() {

  beforeEach(module('shine.menu'));

  describe('menu controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var MenuCtrl = $controller('MenuCtrl');
      expect(MenuCtrl).toBeDefined();
    }));

  });
});